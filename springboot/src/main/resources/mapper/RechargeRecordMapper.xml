<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studyroom.mapper.RechargeRecordMapper">
    
    <select id="selectByUserId" resultType="com.studyroom.entity.RechargeRecord">
        SELECT * FROM recharge_record WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>
    
    <insert id="insert" parameterType="com.studyroom.entity.RechargeRecord" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO recharge_record (user_id, amount, payment_method, payment_status, transaction_id, create_time, update_time)
        VALUES (#{userId}, #{amount}, #{paymentMethod}, #{paymentStatus}, #{transactionId}, #{createTime}, #{updateTime})
    </insert>
    
    <update id="updateById" parameterType="com.studyroom.entity.RechargeRecord">
        UPDATE recharge_record 
        SET payment_status = #{paymentStatus}, 
            transaction_id = #{transactionId}, 
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>
    
    <select id="selectById" resultType="com.studyroom.entity.RechargeRecord">
        SELECT * FROM recharge_record WHERE id = #{id}
    </select>
    
</mapper>
