<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studyroom.mapper.UserBalanceMapper">
    
    <select id="selectByUserId" resultType="com.studyroom.entity.UserBalance">
        SELECT * FROM user_balance WHERE user_id = #{userId}
    </select>
    
    <insert id="insert" parameterType="com.studyroom.entity.UserBalance" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_balance (user_id, balance, frozen_balance, total_recharge, create_time, update_time)
        VALUES (#{userId}, #{balance}, #{frozenBalance}, #{totalRecharge}, #{createTime}, #{updateTime})
    </insert>
    
    <update id="updateById" parameterType="com.studyroom.entity.UserBalance">
        UPDATE user_balance 
        SET balance = #{balance}, 
            frozen_balance = #{frozenBalance}, 
            total_recharge = #{totalRecharge}, 
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>
    
    <select id="selectById" resultType="com.studyroom.entity.UserBalance">
        SELECT * FROM user_balance WHERE id = #{id}
    </select>
    
</mapper>
